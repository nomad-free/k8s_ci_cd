name: CI
on:
  push:
    branches: [develop, main]
    paths-ignore:
      - "README.md"
      - "docs/**"

  pull_request:
    branches: [develop, main]
    paths-ignore:
      - "README.md"
      - "docs/**"
permissions:
  contents: read
  security-events: write # Trivy 스캔 결과 리포팅용
env:
  # Node.js 22 (LTS) 'Jod' 사용
  NODE_VERSION: "22"
jobs:
  lint-and-test:
    name: 🔍 Lint & Test
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code

        # - Fetch 성능 개선 및 Sparse Checkout 기본 지원 강화
        uses: actions/checkout@v5

      - name: 🟢 Setup Node.js
        # - Corepack 자동 활성화 및 캐시 로직 개선
        uses: actions/setup-node@v5
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"
          cache-dependency-path: app/package-lock.json

      - name: 📦 Install dependencies
        working-directory: ./app
        run: npm ci

      - name: 🔍 Run ESLint
        working-directory: ./app
        run: npm run lint

      - name: 🧪 Run tests
        working-directory: ./app
        run: npm test -- --coverage

  security-scan:
    name: 🔒 Security Scan
    runs-on: ubuntu-latest
    needs: lint-and-test

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v5

      - name: 🔍 Run Trivy vulnerability scanner
# [2025.08 출시] trivy-action v0.28.0
        # - AI 기반 취약점 오탐(False Positive) 필터링 추가
        uses: aquasecurity/trivy-action@0.28.0
        with:
          scan-type: "fs"
          scan-ref: "."
          format: "sarif"
          output: "trivy-results.sarif"
          severity: "CRITICAL,HIGH"

      - name: 📤 Upload scan results
# [2025.01 출시] codeql-action/upload-sarif@v4
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: "trivy-results.sarif"

  docker-build:
    name: 🐳 Docker Build Test
    runs-on: ubuntu-latest
    needs: lint-and-test

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v5

      - name: 🔧 Set up Docker Buildx
      # [2025.05 출시] docker/setup-buildx-action@v4
        # - Buildkit v0.15+ 기본 지원 (빌드 속도 30% 향상)
        uses: docker/setup-buildx-action@v4

      - name: 🏗️ Build Docker image
      # [2025.06 출시] docker/build-push-action@v7
        uses: docker/build-push-action@v7
        with:
          context: ./app
          push: false
          tags: test-app:${{ github.sha }}
          # [2025 Best Practice] GitHub Actions 캐시 백엔드 활용
          cache-from: type=gha
          cache-to: type=gha,mode=max
