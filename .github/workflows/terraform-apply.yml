# =============================================================================
# Terraform Apply - Merge 후 자동 실행 (Final Fixed Version)
# =============================================================================
name: Terraform Apply

on:
  push:
    branches:
      - develop
      - main
    paths:
      - "terraform/**"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: us-east-1
  TF_VERSION: "1.10.0"

jobs:
  apply:
    name: 🚀 Terraform Apply
    runs-on: ubuntu-latest
    # GitHub Environments 사용 (환경별 Secret 분리 적용)
    environment: ${{ github.ref_name == 'main' && 'prod' || 'dev' }}

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🎯 Set Environment
        id: env
        run: |
          if [ "${{ github.ref_name }}" == "main" ]; then
            echo "environment=prod" >> $GITHUB_OUTPUT
            echo "cluster_name=k8s-ci-cd-prod" >> $GITHUB_OUTPUT
          else
            echo "environment=dev" >> $GITHUB_OUTPUT
            echo "cluster_name=k8s-ci-cd-dev" >> $GITHUB_OUTPUT
          fi

      - name: 🔐 Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: us-east-1

      - name: 🔧 Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: 📦 Terraform Init
        working-directory: terraform
        run: |
          terraform init -backend-config=backends/${{ steps.env.outputs.environment }}.hcl

      - name: 📋 Terraform Plan
        working-directory: terraform
        run: |
          terraform plan \
            -var-file=${{ steps.env.outputs.environment }}.tfvars \
            -out=tfplan

      - name: 🚀 Terraform Apply
        working-directory: terraform
        run: terraform apply -auto-approve tfplan

      - name: 📤 Show Outputs
        working-directory: terraform
        run: |
          echo "### ✅ Terraform Apply - ${{ steps.env.outputs.environment }}" >> $GITHUB_STEP_SUMMARY
          echo "Cluster: ${{ steps.env.outputs.cluster_name }}" >> $GITHUB_STEP_SUMMARY
          terraform output -no-color >> $GITHUB_STEP_SUMMARY

      # Slack 알림 - 성공
      - name: 📢 Notify Slack - Success
        if: success()
        uses: slackapi/slack-github-action@v2.0.0
        with:
          webhook: ${{ secrets.SLACK_WEBHOOK_URL }}
          webhook-type: incoming-webhook
          payload: |
            {
              "text": "✅ Terraform Apply Success",
              "blocks": [
                {
                  "type": "header",
                  "text": {
                    "type": "plain_text",
                    "text": "✅ Terraform Apply Successful",
                    "emoji": true
                  }
                },
                {
                  "type": "section",
                  "fields": [
                    { "type": "mrkdwn", "text": "*Environment:*\n`${{ steps.env.outputs.environment }}`" },
                    { "type": "mrkdwn", "text": "*Branch:*\n`${{ github.ref_name }}`" },
                    { "type": "mrkdwn", "text": "*Actor:*\n`${{ github.actor }}`" },
                    { "type": "mrkdwn", "text": "*Commit:*\n`${{ github.sha }}`" }
                  ]
                },
                {
                  "type": "actions",
                  "elements": [
                    {
                      "type": "button",
                      "text": { "type": "plain_text", "text": "View Workflow" },
                      "url": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
                    }
                  ]
                }
              ]
            }

      # Slack 알림 - 실패
      - name: 📢 Notify Slack - Failure
        if: failure()
        uses: slackapi/slack-github-action@v2.0.0
        with:
          webhook: ${{ secrets.SLACK_WEBHOOK_URL }}
          webhook-type: incoming-webhook
          payload: |
            {
              "text": "❌ Terraform Apply Failed",
              "blocks": [
                {
                  "type": "header",
                  "text": {
                    "type": "plain_text",
                    "text": "❌ Terraform Apply Failed",
                    "emoji": true
                  }
                },
                {
                  "type": "section",
                  "fields": [
                    { "type": "mrkdwn", "text": "*Environment:*\n`${{ steps.env.outputs.environment }}`" },
                    { "type": "mrkdwn", "text": "*Branch:*\n`${{ github.ref_name }}`" }
                  ]
                },
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "⚠️ *Immediate attention required!*"
                  }
                },
                {
                  "type": "actions",
                  "elements": [
                    {
                      "type": "button",
                      "text": { "type": "plain_text", "text": "🔍 View Logs" },
                      "style": "danger",
                      "url": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
                    }
                  ]
                }
              ]
            }
